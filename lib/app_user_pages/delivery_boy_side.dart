import 'package:flutter/material.dart';

class DeliveryBoySide extends StatelessWidget {
  const DeliveryBoySide({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('delivery boy side'),
      ),
    );
  }
}
