import 'package:flutter/material.dart';

class ClientSide extends StatelessWidget {
  const ClientSide({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Client side'),),
    );
  }
}
